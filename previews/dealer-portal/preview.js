const theme=document.querySelector('#theme');theme.addEventListener('click',()=>{const dark=document.documentElement.dataset.theme!=='dark';document.documentElement.dataset.theme=dark?'dark':'light';theme.textContent=dark?'切換淺色':'切換深色';theme.setAttribute('aria-pressed',String(dark));});
const box=document.querySelector('#lightbox');document.querySelectorAll('.image-open').forEach(button=>button.addEventListener('click',()=>{const img=button.querySelector('img');box.querySelector('img').src=img.src;box.querySelector('img').alt=img.alt;box.querySelector('p').textContent=img.alt;box.showModal();}));document.querySelector('#close-image').addEventListener('click',()=>box.close());box.addEventListener('click',e=>{if(e.target===box)box.close();});
